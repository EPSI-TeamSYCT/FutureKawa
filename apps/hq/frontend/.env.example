# Base URL of the central HQ REST API.
# Leave empty (or set VITE_USE_MOCKS=true) to run fully on the MSW mock layer.
VITE_API_URL=

# When "true", the app boots the Mock Service Worker layer instead of hitting a real API.
# This is the default for local development and the offline jury demo.
VITE_USE_MOCKS=true
