# The frontend talks to the HQ backend through the /hq reverse proxy
# (see vite.config.ts for dev, nginx.conf for prod). Set the dev proxy target
# with HQ_API_TARGET (defaults to http://localhost:3000).

# Optional same-origin base for apiGet; leave empty when using the /hq proxy.
VITE_API_URL=
