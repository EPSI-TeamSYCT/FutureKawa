# @futurekawa/contracts

Shared, language-neutral contracts used across the monorepo (firmware, backends).
Each contract is documented in Markdown and, where machine-readable validation is
useful, backed by a JSON Schema.

## Contents

- [`mqtt/`](mqtt/) — MQTT topics and message payloads exchanged between the IoT
  modules and the country backends.
